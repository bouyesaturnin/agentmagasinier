[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py shell -c \"exec(open('wms/fixtures.py').read())\" && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"